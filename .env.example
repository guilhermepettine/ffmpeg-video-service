# ID do vídeo base no Google Drive (usado como padrão se não for passado no /render)
GOOGLE_DRIVE_VIDEO_ID=

# JSON da service account do Google (conteúdo completo em uma linha)
GOOGLE_SERVICE_ACCOUNT_JSON=

# Porta (Railway injeta automaticamente — não precisa configurar no Railway)
PORT=8000
